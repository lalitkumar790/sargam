name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Build and Test
        run: |
          # Add commands to build and test your web application here
          # For example:
          # npm install
          # npm run build
          # npm test
